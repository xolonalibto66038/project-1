<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" width="128" height="128">
  <!-- Outer white circle -->
  <circle cx="64" cy="64" r="64" fill="#ffffff"/>
  <!-- Inner colored circle — change fill to any color you want -->
  <circle cx="64" cy="64" r="58" fill="#dc3545"/>
  <!-- WN text — change fill to change text color -->
  <text
    x="64"
    y="64"
    text-anchor="middle"
    dominant-baseline="central"
    font-family="DejaVu Sans, Arial, sans-serif"
    font-weight="bold"
    font-size="54"
    fill="#ffffff"
  >WN</text>
</svg>
